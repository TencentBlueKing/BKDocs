
# 20260601

## 标准运维

### bk_sops-3.34.11.2186

这是 **安全更新**，修复了安全问题，请尽快安排更新。
<!-- 版本日志见 GitHub_URL 。-->

#### 在中控机更新
在 **中控机** 下载：
``` bash
bkdl-7.2-stable.sh -ur latest bk_sops=3.34.11.2186
```
在 **中控机** 更新：
``` bash
cd $INSTALL_DIR/blueking/  # 进入工作目录
scripts/setup_bkce7.sh -i sops -f
```
#### 在浏览器里下载并更新
下载适用于蓝鲸 7.x 的安装包：
* [bk_sops-V3.34.11.2186.tar.gz](https://bkopen-1252002024.file.myqcloud.com/saas-paas3/bk_sops/bk_sops-V3.34.11.2186.tar.gz)

参考 《[更新安装包](../manual-install-saas.md#更新安装包)》文档上传安装包，无需调整环境变量，参考《[部署标准运维（bk_sops）](../manual-install-saas.md#部署标准运维（bk_sops）)》文档重新部署。

部署成功后，即可在桌面访问了。

## 流程服务

### bk_itsm-2.8.0-rc.716

这是 **安全更新**，修复了安全问题，请尽快安排更新。
<!-- 版本日志见 GitHub_URL 。-->

#### 在中控机更新
在 **中控机** 下载：
``` bash
bkdl-7.2-stable.sh -ur latest bk_itsm=2.8.0-rc.716
```
在 **中控机** 更新：
``` bash
cd $INSTALL_DIR/blueking/  # 进入工作目录
scripts/setup_bkce7.sh -i itsm -f
```
#### 在浏览器里下载并更新
下载适用于蓝鲸 7.x 的安装包：
* [bk_itsm-V2.8.0-rc.716.tar.gz](https://bkopen-1252002024.file.myqcloud.com/saas-paas3/bk_itsm/bk_itsm-V2.8.0-rc.716.tar.gz)

参考 《[更新安装包](../manual-install-saas.md#更新安装包)》文档上传安装包，无需调整环境变量，参考《[部署流程服务（bk_itsm）](../manual-install-saas.md#部署流程服务（bk_itsm）)》文档重新部署。

部署成功后，即可在桌面访问了。


# 20260605

## 标准运维

### bk_sops-3.34.12.2188

这是 **安全更新**，修复了安全问题，请尽快安排更新。
<!-- 版本日志见 GitHub_URL 。-->

#### 在中控机更新
在 **中控机** 下载：
``` bash
bkdl-7.2-stable.sh -ur latest bk_sops=3.34.12.2188
```
在 **中控机** 更新：
``` bash
cd $INSTALL_DIR/blueking/  # 进入工作目录
scripts/setup_bkce7.sh -i sops -f
```
#### 在浏览器里下载并更新
下载适用于蓝鲸 7.x 的安装包：
* [bk_sops-V3.34.12.2188.tar.gz](https://bkopen-1252002024.file.myqcloud.com/saas-paas3/bk_sops/bk_sops-V3.34.12.2188.tar.gz)

参考 《[更新安装包](../manual-install-saas.md#更新安装包)》文档上传安装包，无需调整环境变量，参考《[部署标准运维（bk_sops）](../manual-install-saas.md#部署标准运维（bk_sops）)》文档重新部署。

部署成功后，即可在桌面访问了。


# 20260612

## 标准运维

### bk_sops-3.34.13.2194

这是 **安全更新**，修复了安全问题，请尽快安排更新。
<!-- 版本日志见 GitHub_URL 。-->

#### 在中控机更新
在 **中控机** 下载：
``` bash
bkdl-7.2-stable.sh -ur latest bk_sops=3.34.13.2194
```
在 **中控机** 更新：
``` bash
cd $INSTALL_DIR/blueking/  # 进入工作目录
scripts/setup_bkce7.sh -i sops -f
```
#### 在浏览器里下载并更新
下载适用于蓝鲸 7.x 的安装包：
* [bk_sops-V3.34.13.2194.tar.gz](https://bkopen-1252002024.file.myqcloud.com/saas-paas3/bk_sops/bk_sops-V3.34.13.2194.tar.gz)

参考 《[更新安装包](../manual-install-saas.md#更新安装包)》文档上传安装包，无需调整环境变量，参考《[部署标准运维（bk_sops）](../manual-install-saas.md#部署标准运维（bk_sops）)》文档重新部署。

部署成功后，即可在桌面访问了。

## 用户管理

### bk-user-1.4.14-beta.36

这是 **补丁更新**，修复了 bkiam-saas-synchronization 服务报错 “sync_organization error” 问题。
<!-- 版本日志见 GitHub_URL 。-->

登录到 **中控机**，先更新 helm 仓库缓存：
``` bash
helm repo update
```
检查仓库里的版本：
``` bash
helm search repo bk-user --version 1.4.14-beta.36
```
预期输出如下所示：
>``` plain
>NAME              CHART VERSION   APP VERSION  DESCRIPTION
>blueking/bk-user  1.4.14-beta.36  略             略
>```

接下来开始升级了。

先进入工作目录：
``` bash
cd $INSTALL_DIR/blueking/  # 进入工作目录
```

修改 `environments/default/version.yaml` 文件，配置 bk-user charts version 为 `1.4.14-beta.36`：
``` bash
sed -i 's/bk-user:.*/bk-user: "1.4.14-beta.36"/' environments/default/version.yaml
grep bk-user environments/default/version.yaml  # 检查修改结果
```
预期输出：
>``` yaml
>  bk-user: "1.4.14-beta.36"
>```

更新 bk-user：
``` bash
helmfile -f base-blueking.yaml.gotmpl -l name=bk-user sync
```

等待命令执行完毕，结尾输出如下即为更新成功：
>``` plain
>UPDATED RELEASES:
>NAME     CHART             VERSION
>bk-user  blueking/bk-user  1.4.14-beta.36
>```

## 监控平台

### bk-monitor-3.9.0-beta.33

这是 **安全更新**，修复了安全问题，请尽快安排更新。
<!-- 版本日志见 GitHub_URL 。-->

登录到 **中控机**，先更新 helm 仓库缓存：
``` bash
helm repo update
```
检查仓库里的版本：
``` bash
helm search repo bk-monitor --version 3.9.0-beta.33
```
预期输出如下所示：
>``` plain
>NAME                 CHART VERSION  APP VERSION  DESCRIPTION
>blueking/bk-monitor  3.9.0-beta.33  略             略
>```

接下来开始升级了。

先进入工作目录：
``` bash
cd $INSTALL_DIR/blueking/  # 进入工作目录
```

修改 `environments/default/version.yaml` 文件，配置 bk-monitor charts version 为 `3.9.0-beta.33`：
``` bash
sed -i 's/bk-monitor:.*/bk-monitor: "3.9.0-beta.33"/' environments/default/version.yaml
grep bk-monitor environments/default/version.yaml  # 检查修改结果
```
预期输出：
>``` yaml
>  bk-monitor: "3.9.0-beta.33"
>```

更新 bk-monitor：
``` bash
helmfile -f 04-bkmonitor.yaml.gotmpl -l name=bk-monitor sync
```

等待命令执行完毕，结尾输出如下即为更新成功：
>``` plain
>UPDATED RELEASES:
>NAME        CHART                VERSION
>bk-monitor  blueking/bk-monitor  3.9.0-beta.33
>```


# 20260618

## 作业平台

### bk-job-0.7.4-beta.6

这是 **补丁更新**，修复了一些问题。
<!-- 版本日志见 GitHub_URL 。-->

登录到 **中控机**，先更新 helm 仓库缓存：
``` bash
helm repo update
```
检查仓库里的版本：
``` bash
helm search repo bk-job --version 0.7.4-beta.6
```
预期输出如下所示：
>``` plain
>NAME             CHART VERSION  APP VERSION  DESCRIPTION
>blueking/bk-job  0.7.4-beta.6   略             略
>```

接下来开始升级了。

先进入工作目录：
``` bash
cd $INSTALL_DIR/blueking/  # 进入工作目录
```

修改 `environments/default/version.yaml` 文件，配置 bk-job charts version 为 `0.7.4-beta.6`：
``` bash
sed -i 's/bk-job:.*/bk-job: "0.7.4-beta.6"/' environments/default/version.yaml
grep bk-job environments/default/version.yaml  # 检查修改结果
```
预期输出：
>``` yaml
>  bk-job: "0.7.4-beta.6"
>```

更新 bk-job：
``` bash
helmfile -f base-blueking.yaml.gotmpl -l name=bk-job sync
```

等待命令执行完毕，结尾输出如下即为更新成功：
>``` plain
>UPDATED RELEASES:
>NAME    CHART            VERSION
>bk-job  blueking/bk-job  0.7.4-beta.6
>```


# 20260622

## 节点管理

### bk-nodeman-2.4.12-rc.2148

这是 **安全更新**，修复了安全问题，请尽快安排更新。
<!-- 版本日志见 GitHub_URL 。-->

登录到 **中控机**，先更新 helm 仓库缓存：
``` bash
helm repo update
```
检查仓库里的版本：
``` bash
helm search repo bk-nodeman --version 2.4.12-rc.2148
```
预期输出如下所示：
>``` plain
>NAME                 CHART VERSION   APP VERSION  DESCRIPTION
>blueking/bk-nodeman  2.4.12-rc.2148  略           略
>```

接下来开始升级了。

先进入工作目录：
``` bash
cd $INSTALL_DIR/blueking/  # 进入工作目录
```

修改 `environments/default/version.yaml` 文件，配置 bk-nodeman charts version 为 `2.4.12-rc.2148`：
``` bash
sed -i 's/bk-nodeman:.*/bk-nodeman: "2.4.12-rc.2148"/' environments/default/version.yaml
grep bk-nodeman environments/default/version.yaml  # 检查修改结果
```
预期输出：
>``` yaml
>  bk-nodeman: "2.4.12-rc.2148"
>```

更新 bk-nodeman：
``` bash
helmfile -f base-blueking.yaml.gotmpl -l name=bk-nodeman sync
```

等待命令执行完毕，结尾输出如下即为更新成功：
>``` plain
>UPDATED RELEASES:
>NAME        CHART                VERSION
>bk-nodeman  blueking/bk-nodeman  2.4.12-rc.2148
>```

>**注意**
>
>我们在 bk-nodeman-2.4.8 调整了 py36.tgz 和 nginx-portable.tgz 的文件名。<br />
>如有安装 GSE Proxy 的需求，请执行如下命令重新下载并上传文件：<br />
>`bkdl-7.2-stable.sh -ur latest gse_proxy && cd $INSTALL_DIR/blueking && ./scripts/setup_bkce7.sh -u opentools`

